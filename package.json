{
  "name": "demo-fastify",
  "version": "1.0.0",
  "description": "demo project for using fastify features",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "start:dev": "fastify start -l info -P -w app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "5.8.0",
    "@fastify/sensible": "5.5.0",
    "fastify": "4.25.1",
    "fastify-plugin": "4.5.1",
    "fluent-json-schema": "4.2.1"
  },
  "engines": {
    "node": ">=20.x.x",
    "npm": ">=10.x.x"
  },
  "devDependencies": {
    "fastify-cli": "^5.9.0"
  }
}
